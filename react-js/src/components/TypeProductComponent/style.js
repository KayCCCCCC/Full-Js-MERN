import styled from "styled-components";

export const WrapperType = styled.div`
  margin-bottom: 5px; /* Change marginBottom to margin-bottom */
  cursor: pointer;
  &:hover {
    background-color: var(--primary-color);
    color: #fff;
    border-radius: 4px;
  }
`;
